---
phase: 02-design-system-and-animation
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - astro-site/src/components/islands/CopyButton.svelte
  - astro-site/src/components/islands/FavoriteButton.svelte
  - astro-site/src/components/islands/ThemeSwitcher.svelte
  - astro-site/tests/animation-a11y.spec.ts
autonomous: false
requirements:
  - DSGN-01
  - DSGN-04
  - DSGN-06

must_haves:
  truths:
    - "CopyButton has spring bounce feedback on copy action"
    - "CopyButton shows 'Copy this emoji' label (warm casual voice) and 'Copied!' feedback"
    - "FavoriteButton has toybox styling with 56px touch target and spring hover"
    - "ThemeSwitcher buttons meet 56px touch target minimum"
    - "Reduced-motion users see instant state changes without bounce animation"
    - "All three Svelte island components use toybox visual language (thick borders, rounded corners, pop colors)"
    - "User visually confirms the design feels toybox-bold and alive"
  artifacts:
    - path: "astro-site/src/components/islands/CopyButton.svelte"
      provides: "Toybox-styled copy button with spring bounce feedback"
      min_lines: 50
    - path: "astro-site/src/components/islands/FavoriteButton.svelte"
      provides: "Toybox-styled favorite button with spring hover"
      min_lines: 30
    - path: "astro-site/src/components/islands/ThemeSwitcher.svelte"
      provides: "Toybox-styled theme switcher with 56px touch targets"
    - path: "astro-site/tests/animation-a11y.spec.ts"
      provides: "Playwright tests for DSGN-04 (reduced-motion emulation, GPU-only properties)"
  key_links:
    - from: "astro-site/src/components/islands/CopyButton.svelte"
      to: "svelte/motion"
      via: "Svelte 5 Spring class for bounce feedback"
      pattern: "import.*Spring.*from.*svelte/motion"
    - from: "astro-site/src/components/islands/CopyButton.svelte"
      to: "astro-site/src/styles/global.css"
      via: "CSS custom properties --radius-xl, --border-soft, --shadow-sm, --touch-min"
      pattern: "var\\(--radius-xl\\)|var\\(--touch-min\\)"
---

<objective>
Restyle CopyButton, FavoriteButton, and ThemeSwitcher Svelte island components with the toybox visual language. Add spring bounce feedback to copy action using Svelte 5 Spring class. Create Playwright tests for animation accessibility (reduced-motion, GPU-only properties). Conclude with visual verification checkpoint.

Purpose: Completes the interactive component visual overhaul. These three components are the primary touch points for user interaction -- they must feel "alive" and match the toybox-bold aesthetic.
Output: Restyled Svelte islands, animation accessibility tests, user-verified visual design.
</objective>

<execution_context>
@/home/sionnach/.claude/get-shit-done/workflows/execute-plan.md
@/home/sionnach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-and-animation/02-CONTEXT.md
@.planning/phases/02-design-system-and-animation/02-RESEARCH.md
@.planning/phases/02-design-system-and-animation/02-01-SUMMARY.md

@astro-site/src/components/islands/CopyButton.svelte
@astro-site/src/components/islands/FavoriteButton.svelte
@astro-site/src/components/islands/ThemeSwitcher.svelte
@astro-site/src/styles/global.css

<interfaces>
<!-- CopyButton.svelte props -->
let { emoji, hex, annotation }: { emoji: string; hex: string; annotation: string } = $props();
<!-- Uses: formatCopyValue, COPY_MODES, CopyMode from '../../lib/utils/copy-formats' -->
<!-- Uses: getCopyMode, setCopyMode from '../../lib/state/copy-mode.svelte' -->
<!-- Uses: addRecent from '../../lib/state/recents.svelte' -->

<!-- FavoriteButton.svelte props -->
let { hex }: { hex: string } = $props();
<!-- Uses: toggleFavorite, isFavorite from '../../lib/state/favorites.svelte' -->

<!-- ThemeSwitcher.svelte props: none (self-contained) -->
<!-- Uses: getTheme, setTheme from '../../lib/state/theme.svelte' -->

<!-- Svelte 5 Spring class API (from svelte/motion): -->
<!-- import { Spring } from 'svelte/motion'; -->
<!-- const scale = new Spring(1, { stiffness: 0.3, damping: 0.4 }); -->
<!-- scale.target = 1.15; // bounces to 1.15 then settles to target -->
<!-- scale.current // reactive current value -->

<!-- NOTE: Svelte 5.7+ has prefersReducedMotion from 'svelte/motion' -->
<!-- import { prefersReducedMotion } from 'svelte/motion'; -->
<!-- if (!prefersReducedMotion.current) { ... animate ... } -->

<!-- Design tokens available from global.css (Plan 01): -->
<!-- --radius-xl: 24px, --radius-md: 16px, --border-soft, --border-strong -->
<!-- --shadow-sm, --shadow-hover, --touch-min: 56px -->
<!-- --spring-bounce, --spring-duration -->
<!-- --pop-mint, --pop-violet, --pop-coral -->
<!-- --bg-elevated, --bg-soft, --text-strong, --text-body, --text-muted -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle CopyButton with spring bounce feedback and toybox visual language</name>
  <files>astro-site/src/components/islands/CopyButton.svelte</files>
  <action>
Update CopyButton.svelte with toybox styling and spring bounce copy feedback.

**Script changes:**
1. Add import: `import { Spring, prefersReducedMotion } from 'svelte/motion';`
2. Create spring instance: `const bounceScale = new Spring(1, { stiffness: 0.3, damping: 0.4 });`
3. In `handleCopy()`, after the successful copy and `copied = true`, add bounce feedback:
```typescript
if (!prefersReducedMotion.current) {
  bounceScale.target = 1.15;
  setTimeout(() => { bounceScale.target = 1; }, 150);
}
```
4. Update copy text to warm casual voice: change "Click to copy" to "Copy this emoji". Change "Copied!" display text to keep as-is (already warm).

**Markup changes:**
1. Add `style:transform="scale({bounceScale.current})"` to the `.copy-btn` button element.
2. Update the copy hint text from "Click to copy" to "Copy this emoji" (DSGN-06 warm casual voice).

**Style changes (replace existing style block entirely):**
Restyle for toybox visual language. Key changes:
- `.copy-btn`: Use `border: 3px solid var(--border-soft)`, `border-radius: var(--radius-md)` (16px), `background: var(--bg-elevated)`, `box-shadow: var(--shadow-xs)`, `min-height: var(--touch-min)`, `padding: 0.75rem 1.25rem`. Transition on transform and box-shadow using spring easing. Remove the old `border-right: none` and split border-radius (was rounded only on left side for the copy/mode button pair).
- `.copy-btn:hover`: `box-shadow: var(--shadow-sm)`, subtle `transform: translateY(-1px)`.
- `.copy-btn.copied`: `background: color-mix(in oklab, var(--pop-mint), var(--bg-elevated) 85%)`, `border-color: var(--pop-mint)`.
- `.copy-feedback`: `color: var(--text-strong)`, `font-weight: 700`.
- `.mode-toggle`: `border: 3px solid var(--border-soft)`, `border-radius: var(--radius-md)`, `min-height: var(--touch-min)`, `background: var(--bg-elevated)`, `box-shadow: var(--shadow-xs)`. Same toybox feel.
- `.mode-toggle:hover`: `box-shadow: var(--shadow-sm)`.
- `.copy-main`: `display: flex`, `gap: 0.5rem` (space between copy and mode buttons instead of joined).
- `.mode-selector`: `border: 3px solid var(--border-soft)`, `border-radius: var(--radius-md)`, `background: var(--bg-elevated)`, `box-shadow: var(--shadow-sm)`.
- `.mode-option`: `min-height: var(--touch-min)`, `padding: 0.65rem 1rem`, `border-bottom: 2px solid color-mix(in oklab, var(--border-soft), transparent 60%)`.
- `.mode-option.active`: `background: color-mix(in oklab, var(--pop-violet), var(--bg-elevated) 90%)`.
- `.copy-value code`: `font-family: var(--font-mono)`, `font-size: 1rem`.
- `.copy-hint`: `font-size: 0.78rem`, `color: var(--text-muted)`.

The copy and mode buttons should now be SEPARATE toybox tiles (not joined into one bar). Each has its own border, radius, and shadow. This matches the "physical toy tiles" metaphor from CONTEXT.md.
  </action>
  <verify>
    <automated>cd /home/sionnach/ws/emoj.ie/astro-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CopyButton has spring bounce feedback on copy (scale 1.15 then settle)
    - Bounce skipped when prefersReducedMotion is active
    - Copy hint reads "Copy this emoji" (warm casual voice)
    - Copied state shows green-mint background with "Copied!" text
    - Copy and mode buttons are separate toybox tiles (not joined)
    - All interactive elements meet 56px min-height touch target
    - Toybox styling: 3px borders, 16px radius, hard shadows, spring transitions
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle FavoriteButton, ThemeSwitcher, and create animation accessibility tests</name>
  <files>astro-site/src/components/islands/FavoriteButton.svelte, astro-site/src/components/islands/ThemeSwitcher.svelte, astro-site/tests/animation-a11y.spec.ts</files>
  <action>
**FavoriteButton.svelte restyling:**

Update styles for toybox visual language. Keep script and markup the same (logic is fine).

Style changes:
- `.favorite-btn`: `border: 3px solid var(--border-soft)`, `border-radius: var(--radius-md)`, `padding: 0.6rem 1rem`, `background: var(--bg-elevated)`, `box-shadow: var(--shadow-xs)`, `min-height: var(--touch-min)`, `font-size: 0.95rem`, `color: var(--text-body)`, `font-family: inherit`. Add `transition: transform var(--spring-duration, 500ms) var(--spring-bounce, ease), box-shadow var(--spring-duration, 500ms) var(--spring-bounce, ease)`.
- `.favorite-btn:hover`: `transform: translateY(-2px)`, `box-shadow: var(--shadow-sm)`.
- `.favorite-btn:focus-visible`: Same as hover.
- `.favorite-btn.active`: `border-color: var(--pop-coral)`, `background: color-mix(in oklab, var(--pop-coral), var(--bg-elevated) 88%)`.
- `.favorite-icon`: `font-size: 1.2rem`.

**ThemeSwitcher.svelte restyling:**

Update styles for toybox visual language and 56px touch targets.

Style changes:
- `.theme-switcher`: `display: flex`, `gap: 0.35rem`, `border: 3px solid var(--border-soft)`, `border-radius: var(--radius-md)`, `padding: 0.25rem`, `background: var(--bg-elevated)`, `box-shadow: var(--shadow-xs)`.
- `.theme-btn`: `display: flex`, `align-items: center`, `justify-content: center`, `width: 48px`, `height: var(--touch-min)` (56px -- meeting DSGN-03), `border: 2px solid transparent`, `border-radius: calc(var(--radius-md) - 4px)` (inner radius), `background: transparent`, `cursor: pointer`, `font-size: 1.15rem`, `transition: background-color 200ms ease, border-color 200ms ease`.
- `.theme-btn:hover`: `background: var(--bg-soft)`, `border-color: color-mix(in oklab, var(--border-soft), transparent 60%)`.
- `.theme-btn.active`: `background: color-mix(in oklab, var(--pop-violet), var(--bg-elevated) 88%)`, `border-color: var(--pop-violet)`, `box-shadow: var(--shadow-xs)`.
- `.theme-icon`: `line-height: 1`, `font-size: 1.25rem`.

NOTE on ThemeSwitcher sizing: Use `width: 48px` and `height: var(--touch-min)` (56px). The 48px width keeps the switcher compact enough for mobile headers while the 56px height meets DSGN-03 touch target requirements. The height dimension is what matters most for thumb targets.

**Playwright tests (animation-a11y.spec.ts):**

Create `astro-site/tests/animation-a11y.spec.ts` covering DSGN-04:

1. **Reduced motion test:** Use `page.emulateMedia({ reducedMotion: 'reduce' })`. Navigate to a subgroup page (which has EmojiGrid). Verify `.emoji-tile` (or `.panel-card` depending on which the grid now uses) has computed `animation-duration` of `0.01ms` or `animation: none`. Verify transition-duration is `0.15s` (graceful, not nuclear `0.01ms`).

2. **GPU-only properties test:** Navigate to home page. Get computed styles for `.panel-card` transition properties. Verify transition-property only contains `transform`, `box-shadow`, and/or `opacity` -- NOT `width`, `height`, `top`, `left`, `margin`, or `padding`. This can be done by:
   - Evaluating `getComputedStyle(element).transitionProperty` on a `.panel-card`
   - Asserting it does NOT contain layout-triggering properties

3. **Spring easing existence test:** Verify `--spring-bounce` CSS variable exists and contains either `cubic-bezier` (fallback) or `linear(` (enhanced).

4. **Animation on emoji tiles:** Navigate to a subgroup page, verify `.emoji-tile` has `animation-name` containing `card-entrance`.

Use `page.emulateMedia()` for reduced motion. Use `page.evaluate()` for computed style checks. Reference existing Playwright config from smoke.spec.ts.
  </action>
  <verify>
    <automated>cd /home/sionnach/ws/emoj.ie/astro-site && npm run build 2>&1 | tail -5 && npx playwright test tests/animation-a11y.spec.ts --reporter=list 2>&1 | tail -15</automated>
  </verify>
  <done>
    - FavoriteButton has toybox styling: thick border, rounded corners, spring hover, pop-coral active state
    - ThemeSwitcher has toybox styling: bordered container, 48x56px buttons, pop-violet active state
    - All interactive elements meet 56px min-height touch target
    - animation-a11y tests pass: reduced-motion graceful degradation verified, GPU-only properties verified
    - Astro build completes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of toybox-bold design</name>
  <files>none</files>
  <action>
Human verifies the complete toybox-bold visual redesign across the entire site.

What was built:
- Extended design token system (pop colors, larger radii, spring easing, deeper shadows)
- EmojiGrid with emoji-only toybox tiles, stagger entrance animation, hover annotation reveal
- Detail page hero-first layout with huge 200-280px emoji above the fold
- CopyButton with spring bounce feedback and warm casual copy voice
- FavoriteButton and ThemeSwitcher with toybox styling
- Touch targets enforced at 56px on all interactive elements
- Graceful reduced-motion support (brief transitions preserved)

How to verify:
1. Run `cd astro-site && npm run dev` to start the dev server
2. Open http://localhost:4321 in your browser

**Home page check:**
- Category cards should have thick borders, 24px rounded corners, hard shadows
- Hovering a card should produce a bouncy scale-up effect with deeper shadow
- Cards should "spill out" with a stagger cascade animation on page load

**Emoji grid check (click any category, then click a subgroup):**
- Emoji tiles show ONLY the emoji image (no annotation text visible)
- Hovering a tile reveals the annotation label at the bottom
- Tiles have colorful tint variations (mint, violet, coral)
- Tiles animate in with stagger cascade on page load
- Emojis are bigger than before (96-120px)

**Detail page check (click any emoji):**
- Hero emoji should be HUGE (200-280px), centered, dominating above the fold
- Emoji name and copy button visible without scrolling
- Metadata (category, unicode, tags, keywords) is below a separator, requiring scroll

**Copy interaction check:**
- Click "Copy this emoji" button -- should bounce and show "Copied!" with green-mint background
- The bounce should feel springy, not robotic

**Touch target check (on mobile or using responsive mode at 375px width):**
- All buttons (copy, favorite, theme switcher) should feel comfortably tappable
- No tiny click targets

**Reduced motion check:**
- In browser devtools, toggle "Emulate CSS prefers-reduced-motion: reduce"
- Reload a grid page -- tiles should appear instantly (no stagger animation)
- Hover effects should still work but with brief transitions, not bouncy springs

**Overall feel:** Does the site feel "toybox bold" -- chunky, playful, alive? Does it look distinct from emojipedia/competitors? Would a kid AND an adult both enjoy using it?

Resume signal: Type "approved" if the design feels toybox-bold and alive, or describe specific issues to fix.
  </action>
  <verify>User approves the visual design</verify>
  <done>User confirms the site looks and feels toybox-bold, playful, and distinct from competitors</done>
</task>

</tasks>

<verification>
1. `cd astro-site && npm run build` completes without errors
2. `cd astro-site && npx playwright test` -- all tests pass (smoke, design-system, touch-targets, minimal-copy, animation-a11y)
3. User confirms visual design meets toybox-bold aesthetic
</verification>

<success_criteria>
- CopyButton has spring bounce feedback using Svelte 5 Spring class
- CopyButton uses warm casual voice ("Copy this emoji", not "Click to copy")
- CopyButton and mode selector are separate toybox tiles
- FavoriteButton has toybox styling with pop-coral active state
- ThemeSwitcher has toybox container with properly-sized touch targets
- Animation accessibility tests verify reduced-motion and GPU-only properties
- User visually approves the complete toybox-bold design
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-and-animation/02-03-SUMMARY.md`
</output>
