// The whole site is prerendered static HTML (GitHub Pages).
export const prerender = true;

// GitHub Pages parity: every route is a directory with index.html.
export const trailingSlash = 'always';
